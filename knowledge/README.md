# SIP website knowledge

`markdown/` contains a reviewed snapshot of public ETIL and IBC Group website content retrieved on 10 September 2026. Each document retains its canonical source URL and source language. The content is used as reference material for local Product Strategist retrieval and is not an approved Business Context.

The current lexical retrieval is deliberately local and deterministic. Before an Azure development deployment, replace it with Azure AI Search while preserving the API response contract and source metadata.
